<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.FormTree" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.FormTree" parent="Base.Theme.FormTree" />

    <!--  style for progress bar -->
    <style name="ProgressBarStyle" parent="android:Theme.DeviceDefault.Dialog">
        <item name="DialogTitleAppearance">@android:style/TextAppearance.Medium</item>
        <item name="DialogSpotColor">@color/black</item>
        <item name="android:textColorPrimary">@color/black</item>
        <item name="android:windowBackground">@drawable/bg_white_curvy</item>
        <item name="DialogSpotCount">4</item>
    </style>
</resources>